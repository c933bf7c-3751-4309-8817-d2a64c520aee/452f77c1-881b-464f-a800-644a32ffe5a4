<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parkir Page</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #f0f0f0;
            margin: 0;
        }

        .box {
            width: 100px;
            height: 100px;
            background-color: green;
            display: inline-block;
            margin: 15px;
            cursor: pointer;
            text-align: center;
            line-height: 100px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: background-color 0.3s, transform 0.3s;
        }

        .box:hover {
            transform: scale(1.05);
        }

        .red {
            background-color: red;
        }

        .status {
            font-size: 1.2em;
            margin-top: 20px;
        }

        #redRemaining {
            color: red;
            margin-bottom: 10px;
        }

        #greenRemaining {
            color: green;
        }
    </style>
</head>
<body>
    <div id="parking-slots">
        <!-- Slot boxes will be dynamically generated here by JavaScript -->
    </div>
    <div class="status" id="redRemaining">Parkir Terpakai: 0</div>
    <div class="status" id="greenRemaining">Parkir Tersedia: 4</div>

    <script type="module" src="/js/parkir-page.js"></script>
</body>
</html>
